---
tags:
  - 求导
---

## 基础知识

规定向量 $f \in \mathbb{R}^{m}$ 对向量 $\boldsymbol{x} \in \mathbb{R}^{n}$ 求导，那么 $\frac{{\partial f}}{\partial \boldsymbol{x}} \in \mathbb{R}^{m \times n}$，当分子或分母出现了矩阵，则使用 $\mathrm{vec}(\boldsymbol{X})$ 将其进行向量化，比如 $\boldsymbol{X} \in \mathbb{R}^{n \times p}$，那么 $\mathrm{vec}(\boldsymbol{X}) \in \mathbb{R}^{np}$，则 $\frac{{\partial f}}{\partial \mathrm{vec}(\boldsymbol{X})} \in \mathbb{R}^{m \times np}$

与[[求导标量篇]]的定理 1 类似，定理 2 相当于每个都套了 $\mathrm{vec}(\cdot)$

> [!theorem] 定理 2
>
> $$
> \begin{aligned}
> \mathrm{vec}(\mathrm{d} f) = \frac{{\partial \mathrm{vec}(f)}}{\partial \mathrm{vec}(\boldsymbol{X})} \mathrm{vec}(\mathrm{d}\boldsymbol{X})
> \end{aligned}
> $$

换句话说，凑出跟定理一样的形式，自然就能求出导数，另外还有些常用法则：

$$
\begin{align*}
\mathrm{vec}(\boldsymbol{A} + \boldsymbol{B}) &= \mathrm{vec}(\boldsymbol{A}) + \mathrm{vec}(\boldsymbol{B}) \\
(\boldsymbol{A} \otimes \boldsymbol{B})^{\top} &= \boldsymbol{A^{\top}} \otimes \boldsymbol{B^{\top}}\\
\mathrm{vec}(\boldsymbol{A} \odot \boldsymbol{B}) &= \text{diag}(\mathrm{vec}(\boldsymbol{A}))\mathrm{vec}(\boldsymbol{B})\\
(\boldsymbol{A} \otimes \boldsymbol{B})(\boldsymbol{C} \otimes \boldsymbol{D}) &= (\boldsymbol{AC}) \otimes (\boldsymbol{BD})\\
\mathrm{vec}(\boldsymbol{ABC})&= (\boldsymbol{C^{\top}}\otimes \boldsymbol{A})\mathrm{vec}(\boldsymbol{B})
\end{align*}
$$

其中 $\otimes$ 是克罗内克积，$\boldsymbol{A}  \in \mathbb{R}^{m \times n}, \boldsymbol{B} \in \mathbb{R}^{p \times q}, \boldsymbol{A}\otimes \boldsymbol{B} \in \mathbb{R}^{mp \times nq}$，举个例子：

$$
\begin{bmatrix}
1 & 2 \\
3 & 4
\end{bmatrix} \otimes \begin{bmatrix}
0 & 1  \\
2 & 3
\end{bmatrix} = \begin{bmatrix}
1\begin{bmatrix}
0 & 1 \\
2 & 3
\end{bmatrix} & 2\begin{bmatrix}
0 & 1 \\
2 & 3
\end{bmatrix} \\
3\begin{bmatrix}
0 & 1 \\
2 & 3
\end{bmatrix} & 4\begin{bmatrix}
0 & 1 \\
2 & 3
\end{bmatrix}
\end{bmatrix} = \begin{bmatrix}
1 \times 0 & 1 \times 1 & 2 \times 0 & 2 \times 1 \\
1 \times 2 & 1 \times 3 & 2 \times 2 & 2 \times 3 \\
3 \times 0 & 3 \times 1 & 4\times 0 & 4 \times 1 \\
3 \times 2 & 3 \times 3 & 4 \times 2 & 4 \times 3
\end{bmatrix}
$$

## 例子

### 开胃菜

这是一个向量对矩阵求导的例子

$$
\frac{{\partial (\boldsymbol{y} \odot (\boldsymbol{Xw}))}}{\partial \boldsymbol{X}}
$$

令 $f = \boldsymbol{y} \odot (\boldsymbol{Xw})$，则：

$$
\begin{align*}
f &= \boldsymbol{y} \odot (\boldsymbol{Xw}) = \text{diag}(\boldsymbol{y})\boldsymbol{Xw}\\
\mathrm{d}f &= \text{diag}(\boldsymbol{y})\mathrm{d}\boldsymbol{Xw}
\end{align*}
$$

两边进行向量化：

$$
\begin{align*}
\mathrm{vec}(\mathrm{d}f) &= \mathrm{vec}(\text{diag}(\boldsymbol{y})\mathrm{d}\boldsymbol{Xw})\\
&= (\boldsymbol{w^{\top}} \otimes \text{diag}(\boldsymbol{y}))\mathrm{vec}(\mathrm{d}\boldsymbol{X})
\end{align*}
$$

那么：

$$
\frac{{\partial \mathrm{vec}(f)}}{\partial \mathrm{vec}(\boldsymbol{X})} = \boldsymbol{w^{\top}} \otimes \text{diag}(\boldsymbol{y})
$$

总结：先求出 $\mathrm{d} \boldsymbol{X}$，再两边套 $\mathrm{vec}(\cdot)$，然后凑形式即可

### Softmax

$\boldsymbol{z} \in \mathbb{R}^{n}, \boldsymbol{W} \in \mathbb{R}^{n \times m}, \boldsymbol{x} \in \mathbb{R}^{m}$，求 $\frac{\partial \boldsymbol{a}}{\partial \boldsymbol{W}}$：

$$
\begin{align*}
\boldsymbol{z} &= \boldsymbol{Wx}\\
\boldsymbol{a} &= \text{softmax}(z)
\end{align*}
$$

$$
\begin{align*}
\mathrm{d} \boldsymbol{z} &= \mathrm{d}\boldsymbol{Wx}\\
\mathrm{vec}(\mathrm{d}\boldsymbol{z}) &= \mathrm{vec}({\color{#08F}\boldsymbol{I}}\mathrm{d}\boldsymbol{Wx})\\
&= (\boldsymbol{x^{\top}} \otimes \boldsymbol{I})\mathrm{vec}(\mathrm{d}\boldsymbol{W})
\end{align*}
$$

则：

$$
\frac{{\partial \boldsymbol{z}}}{\partial \mathrm{vec}(\boldsymbol{W})} = \boldsymbol{x^{\top}} \otimes \boldsymbol{I}\rule{0pt}{1.4em}
$$

接着看 softmax 求导：

$$
\boldsymbol{a}_{i} = \frac{\text{exp}(\boldsymbol{z}_{i})}{\sum_{j}^{n}\text{exp}(z_{j})}
$$

$\boldsymbol{a}$ 对 $\boldsymbol{z}$ 的导数是 $\mathbb{R}^{n \times n}$，i 行 j 列的元素是 $\frac{{\partial \boldsymbol{a}_{i}}}{\partial \boldsymbol{z}_{j}}$，记 $\sum_{j}^{n}\text{exp}(z_{j})=S$

$$
\begin{align*}
i= j&,\, \frac{{\text{exp}(\boldsymbol{z}_{i}) \times S - \text{exp}^{2}(\boldsymbol{z}_{i})}}{S^{2}} = \frac{\text{exp}(\boldsymbol{z}_{i})}{S} - \frac{\text{exp}(\boldsymbol{z}_{i})}{S}\times \frac{\text{exp}(\boldsymbol{z}_{i})}{S}=\boldsymbol{a}_{i} -\boldsymbol{a}_{i}^{2}\\
i \neq j&, \, \frac{{0 - \text{exp}(\boldsymbol{z}_{i})\text{exp}(\boldsymbol{z}_{j})}}{S^{2}} = -\frac{\text{exp}(\boldsymbol{z}_{i})}{S} \times \frac{\text{exp}(\boldsymbol{z}_{j})}{S} = -\boldsymbol{a}_{i}\boldsymbol{a}_{j}
\end{align*}
$$

整理一下，

$$
\frac{{\partial \boldsymbol{a}}}{\partial \boldsymbol{z}} = \text{diag}(\boldsymbol{a}) - \boldsymbol{a}\boldsymbol{a^{\top}}\rule{0pt}{1.4em}
$$

由上式可知，在训练时对 `logits` 进行原地操作对梯度的计算无影响，因为求导用不到

因为参与微分的对象都是*向量*，利用链式求导法则可得，

$$
\begin{align*}
\frac{{\partial \boldsymbol{a}}}{\partial \mathrm{vec}(\boldsymbol{W})} = \frac{{\partial \boldsymbol{a}}}{\partial \boldsymbol{z}} \frac{{\partial \boldsymbol{z}}}{\partial \mathrm{vec}(\boldsymbol{W})}&= (1 \otimes(\text{diag}(\boldsymbol{a}) - \boldsymbol{a}\boldsymbol{a^{\top}}))(\boldsymbol{x^{\top}}\otimes \boldsymbol{I})\rule{0pt}{1.4em}\\
&= \boldsymbol{x^{\top}}\otimes (\text{diag}(\boldsymbol{a}) - \boldsymbol{a}\boldsymbol{a^{\top}}) \in \mathbb{R}^{n\times mn}
\end{align*}
$$

上式用到了 $1 \otimes \boldsymbol{A} = \boldsymbol{A}$ 的性质，$1$ 是标量

> [!warning]
> 链式法则最好只用在参与求导的都是标量或者向量，否则用全微分来解而不是用链式法则
