---
tags:
  - 求导
---

## 基础知识

规定标量 $f \in \mathbb{R}$ 对向量或者矩阵求导，求导的布局如下：

- 分母是 $\boldsymbol{x} \in \mathbb{R}^{n}$，那么 $\frac{{\partial f}}{\partial \boldsymbol{x}} \in \mathbb{R}^{n}$
- 分母是 $\boldsymbol{X} \in \mathbb{R}^{m\times n}$，那么 $\frac{{\partial f}}{\partial \boldsymbol{X}} \in \mathbb{R}^{m \times n}$

> [!theorem] 定理 1
>
> $$
> \begin{aligned}
> \mathrm{d} f = \left(\frac{{\partial f}}{\partial \boldsymbol{x}}\right)^{\top} \mathrm{d}\boldsymbol{x} = \mathrm{Tr}\left(\left(\frac{{\partial f}}{\partial \boldsymbol{x}}\right)^{\top} \mathrm{d}\boldsymbol{x}\right)
> \end{aligned}
> $$

比如 $y=2x$，那么 $\mathrm{d}y = 2 \,\mathrm{d}x$，当 $x$ 变化了 $\mathrm{d}x$，则 $y$ 变化 $2 \mathrm{d}x$，转置是因为 $\mathrm{d}\boldsymbol{x}$ 和求导的布局都跟 $\boldsymbol{x}$ 形状一致，要得出标量 $\mathrm{d} f$，转置必不可少；第二个等号则因为迹的性质：$\mathrm{Tr}(c) = c, c \in \mathbb{R}$，除此之外还有以下法则，其中 $\odot$ 是逐元素相乘

$$
\begin{align*}
\mathrm{Tr}(\boldsymbol{AB}) &= \mathrm{Tr}(\boldsymbol{BA})\\
\mathrm{Tr}(\boldsymbol{A^{\top}}) &= \mathrm{Tr}(\boldsymbol{A})\\
\mathrm{Tr}(\boldsymbol{A} + \boldsymbol{B}) &= \mathrm{Tr}(\boldsymbol{A}) + \mathrm{Tr}(\boldsymbol{B})\\
\mathrm{Tr}(\boldsymbol{A}^{\top}(\boldsymbol{B} \odot \boldsymbol{C})) &= \mathrm{Tr}((\boldsymbol{A} \odot \boldsymbol{B})^{\top}\boldsymbol{C})
\end{align*}
$$

求导时利用迹的性质，将形式转换成定理的模样，那么求导结果自然就能得出。另外当 $\boldsymbol{X}$ 可逆时：

$$
\mathrm{d}(\boldsymbol{X}^{-1}) = -\boldsymbol{X}^{-1} (\mathrm{d}\boldsymbol{X}) \boldsymbol{X}^{-1}
$$

## 例子

### 二次型

$$
f(\boldsymbol{x}) = \boldsymbol{x^{\top}Ax},\, \boldsymbol{x} \in \mathbb{R}^{n}, \boldsymbol{A} \in \mathbb{R}^{n \times n}
$$

$$
\begin{align*}
\mathrm{d} f &= \mathrm{d} \boldsymbol{x}^{\top}\boldsymbol{Ax} + \boldsymbol{x^{\top}A}\mathrm{d} \boldsymbol{x}\\
&= \mathrm{Tr}((\mathrm{d} \boldsymbol{x})^{\top}\boldsymbol{Ax}) + \mathrm{Tr}(\boldsymbol{x^{\top}A}\mathrm{d}\boldsymbol{x})\\
&= \mathrm{Tr}(\boldsymbol{x^{\top}A^{\top}}\mathrm{d}\boldsymbol{x}) + \mathrm{Tr}(\boldsymbol{x^{\top}A}\mathrm{d}\boldsymbol{x})\\
&= \mathrm{Tr}((\boldsymbol{x^{\top}(\boldsymbol{A}^{\top}}+ \boldsymbol{A}))\mathrm{d}\boldsymbol{x})
\end{align*}
$$

则：

$$
\frac{{\partial f}}{\partial \boldsymbol{x}} = (\boldsymbol{x^{\top}}(\boldsymbol{A^{\top}}+ \boldsymbol{A}))^{\top} = (\boldsymbol{A} + \boldsymbol{A^{\top}})\boldsymbol{x}
$$

假如我们对 $\boldsymbol{A}$ 求导：

$$
\mathrm{d}f = \boldsymbol{x^{\top}}\mathrm{d}\boldsymbol{A}\boldsymbol{x} = \mathrm{Tr}(\boldsymbol{xx^{\top}}\mathrm{d}\boldsymbol{A})
$$

则：

$$
\frac{{\partial f}}{\partial \boldsymbol{A}} = (\boldsymbol{xx^{\top}})^{\top} = \boldsymbol{xx^{\top}}\rule{0pt}{1.4em}
$$

### 全连接层

假设有一个简易两层网络，求 $\frac{{\partial \mathcal{L}}}{\partial \boldsymbol{W}_{1}}$

$$
\begin{align*}
\boldsymbol{z}_{1} &= \boldsymbol{W}_{1}\boldsymbol{x}\\
\boldsymbol{a}_{1} &= \sigma(\boldsymbol{z}_{1})\\
\boldsymbol{z}_{2} &= \boldsymbol{W}_{2}\boldsymbol{a}_{1}\\
\mathcal{L} &= \frac{1}{2}\|\boldsymbol{y} - \boldsymbol{z}_{2}\|^{2}
\end{align*}
$$

先将 loss 展开：

$$
\begin{align*}
\mathcal{L} &= \frac{1}{2}(\boldsymbol{y} - \boldsymbol{z}_{2})^{\top}(\boldsymbol{y} - \boldsymbol{z}_{2})\\
&= \frac{1}{2}(\boldsymbol{y^{\top}y} - \boldsymbol{y^{\top}}\boldsymbol{z}_{2}-\boldsymbol{z}_{2}^{\top}\boldsymbol{y} + \boldsymbol{z}_{2}^{\top}\boldsymbol{z}_{2})
\end{align*}
$$

下式推导用到了两向量内积： $\boldsymbol{a^{\top}}\boldsymbol{b} = \boldsymbol{b^{\top}a}$

$$
\begin{align*}
\mathrm{d} \mathcal{L}  & = \frac{1}{2}(-\boldsymbol{y^{\top}}\mathrm{d}\boldsymbol{z}_{2} - \mathrm{d}\boldsymbol{z}_{2}^{\top}\boldsymbol{y} + \mathrm{d}\boldsymbol{z}_{2}^{\top}\boldsymbol{z}_{2} + \boldsymbol{z}_{2}^{\top}\mathrm{d}\boldsymbol{z}_{2})\\
&= (\boldsymbol{z}_{2}-\boldsymbol{y})^{\top}\mathrm{d}\boldsymbol{z}_{2}
\end{align*}
$$

接着用「逐元素」激活函数求导：$\mathrm{d} \boldsymbol{a}_{1} = \sigma'(\boldsymbol{z}_{1})\odot \mathrm{d}\boldsymbol{z}_{1}=\text{diag}(\sigma'(\boldsymbol{z}_{1}))\mathrm{d}\boldsymbol{z}_{1}$，非逐元素不成立，比如 softmax

$$
\begin{align*}
\mathrm{d}\boldsymbol{z}_{2}  & = \boldsymbol{W}_{2}\mathrm{d}\boldsymbol{a}_{1} \\
 & = \boldsymbol{W}_{2}(\sigma'(\boldsymbol{z_{1}})\odot \mathrm{d} \boldsymbol{z}_{1}) \\
 & = \boldsymbol{W}_{2}(\sigma'(\boldsymbol{z}_{1})\odot \mathrm{d}\boldsymbol{W}_{1}\boldsymbol{x})
\end{align*}
$$

然后将结果代入上式，第三个等式用到了 $\boldsymbol{u^{\top}}(\boldsymbol{v}\odot \boldsymbol{w})=(\boldsymbol{u} \odot \boldsymbol{v})^{\top}\boldsymbol{w}$：

$$
\begin{align*}
\mathrm{d}\mathcal{L} &= (\boldsymbol{z}_{2} - \boldsymbol{y})^{\top}(\boldsymbol{W}_{2}(\sigma'(\boldsymbol{z}_{1})\odot \mathrm{d}\boldsymbol{W}_{1}\boldsymbol{x})\\
&= (\boldsymbol{z}_{2}-\boldsymbol{y})^{\top}\boldsymbol{W}_{2}(\sigma'(\boldsymbol{z}_{1})\odot \mathrm{d}\boldsymbol{W}_{1}\boldsymbol{x})\\
&= ((\boldsymbol{W}_{2}^{\top}(\boldsymbol{z}_{2} - \boldsymbol{y}))\odot\sigma'(\boldsymbol{z}_{1}))^{\top}\mathrm{d}\boldsymbol{W}_{1}\boldsymbol{x}\\
&= \mathrm{Tr}(\boldsymbol{x}((\boldsymbol{W}_{2}^{\top}(\boldsymbol{z}_{2} - \boldsymbol{y}))\odot\sigma'(\boldsymbol{z}_{1}))^{\top}\mathrm{d}\boldsymbol{W}_{1})
\end{align*}
$$

则：

$$
\frac{{\partial \mathcal{L}}}{\partial \boldsymbol{W}_{1}} = ((\boldsymbol{W}_{2}^{\top}(\boldsymbol{z}_{2} - \boldsymbol{y}))\odot\sigma'(\boldsymbol{z}_{1}))\boldsymbol{x^{\top}}\rule{0pt}{1.4em}
$$

又因为 $\sigma'(\boldsymbol{z}_{1}) = \sigma(\boldsymbol{z}_{1})\odot(\boldsymbol{1}- \sigma(\boldsymbol{z}_{1}))$，因为这里是向量，所以得用 $\odot$ 以及 $\boldsymbol{1}$ 是全 1 向量

$$
\begin{align*}
\frac{{\partial \mathcal{L}}}{\partial \boldsymbol{W}_{1}} &= \left([\boldsymbol{W}_{2}^{\top}(\boldsymbol{z}_{2} - \boldsymbol{y})]\odot[\sigma(\boldsymbol{z}_{1})\odot(\boldsymbol{1}- \sigma(\boldsymbol{z}_{1}))]\right)\boldsymbol{x^{\top}}\rule{0pt}{1.4em}\\
&= \left([\boldsymbol{W}_{2}^{\top}(\boldsymbol{z}_{2} - \boldsymbol{y})]\odot[\boldsymbol{a}_{1}\odot(\boldsymbol{1}- \boldsymbol{a}_{1})]\right)\boldsymbol{x^{\top}}
\end{align*}
$$

### 逻辑回归

求 $\frac{\partial \mathcal{L}}{\partial \boldsymbol{w}}$，注意 $a, y \in \mathbb{R}$

$$
\begin{align*}
a &= \sigma(\boldsymbol{w}^\top \boldsymbol{x})\\
\mathcal{L} &= -[y\ln a + (1-y)\ln(1-a)]
\end{align*}
$$

首先对 $\mathcal{L}$ 关于 $a$ 求微分：

$$
\begin{align*}
\mathrm{d}\mathcal{L} &= -\left( \frac{y}{a}\mathrm{d}a - \frac{1-y}{1-a}\mathrm{d}a \right) \\
&= -\frac{y(1-a) - a(1-y)}{a(1-a)}\mathrm{d}a \\
&= \frac{a-y}{a(1-a)}\mathrm{d}a
\end{align*}
$$

接着，因为这里是标量，所以去掉了 $\odot$

$$
\mathrm{d}{a} = \sigma'(\boldsymbol{w^{\top}x}) (\mathrm{d}\boldsymbol{w^{\top}x})
$$

代入：

$$
\begin{align*}
\mathrm{d}\mathcal{L} &= \frac{{{y}-{a}}}{{a}({a}-1)}\sigma'(\boldsymbol{w^{\top}x}) (\mathrm{d} \boldsymbol{w^{\top}x})\\
&= \frac{{{y}-{a}}}{{a}({a}-1)}({a}(1-{a})) (\mathrm{d} \boldsymbol{w^{\top}x})\\
&= (a-y)\mathrm{d}\boldsymbol{w^{\top}x}\\
&= \mathrm{Tr}((a-y)(\mathrm{d}\boldsymbol{w})^{\top}\boldsymbol{x})\\
&= \mathrm{Tr}((a-y)\boldsymbol{x^{\top}}\mathrm{d}\boldsymbol{w})
\end{align*}
$$

所以：

$$
\frac{{\partial \mathcal{L}}}{\partial \boldsymbol{w}} = (a-y)\boldsymbol{x}
$$
